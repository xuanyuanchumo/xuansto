{
  "name": "sanliu",
  "version": "1.0.0",
  "description": "三省六部技能 - 测试覆盖率配置",
  "private": true,
  "scripts": {
    "test:coverage": "python scripts/check_coverage.py",
    "test:report": "python scripts/generate_test_report.py",
    "test:backend": "cd backend && python -m pytest --cov=app --cov-report=html:htmlcov --cov-report=term-missing -v",
    "test:frontend": "cd frontend && npm run test:coverage",
    "coverage:check": "python scripts/check_coverage.py",
    "coverage:report": "python scripts/generate_test_report.py"
  },
  "engines": {
    "node": ">=18.0.0",
    "python": ">=3.10"
  }
}
